# Implementation Plan

1. Audit scaffold and preserve authentication/session conventions.
2. Add marketplace schema, domain validation, audit logging, and mockable Shopify GraphQL services.
3. Add authenticated admin pages and creator/application/import workflows.
4. Add authenticated app-proxy APIs and theme extension.
5. Add publishing state machine, webhooks, settings, setup validation, and security controls.
6. Add tests and documentation; run generate, migration, lint, typecheck, tests, and build.

Live-shop checks and merchant Theme Editor actions are documented separately.
