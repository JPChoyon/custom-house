# Test Plan

Local gates: Prisma generate/migrate, domain tests, ESLint, strict TypeScript, and production build. Live-shop acceptance: signed proxy login/logout; duplicate application; admin status/tag changes; collection created once; Global Product validation; saved URL allowlist; publish/retry; required metafields; collection membership; publication; creator-product Add to Cart; Global Product customization unaffected; privacy and deletion webhooks. Use a development store and disposable products/collections.
