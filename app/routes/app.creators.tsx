import type { ActionFunctionArgs, LoaderFunctionArgs } from "react-router"; import { Form, useLoaderData } from "react-router"; import db from "../db.server"; import { authenticate } from "../shopify.server"; import { changeCreatorStatus } from "../services/creator.server"; import { AdminGraphqlClient } from "../services/shopify-graphql.server";
export async function loader({ request }: LoaderFunctionArgs) { const { session } = await authenticate.admin(request); return db.creator.findMany({ where: { shop: session.shop }, include: { _count: { select: { submissions: true } } }, orderBy: { updatedAt: "desc" }, take: 100 }); }
export async function action({ request }: ActionFunctionArgs) { const { session, admin } = await authenticate.admin(request); const form = await request.formData(); const state = String(form.get("status")); if (!(["APPROVED", "REJECTED", "SUSPENDED"] as string[]).includes(state)) throw new Response("Invalid status", { status: 400 }); await changeCreatorStatus(session.shop, String(form.get("creatorId")), state as "APPROVED" | "REJECTED" | "SUSPENDED", new AdminGraphqlClient(admin), String(form.get("reason") || "") || undefined); return { ok: true }; }
export default function Creators() { const creators = useLoaderData<typeof loader>(); return <s-page heading="Creators"><s-section>{creators.map((c) => <s-box key={c.id} padding="base" borderWidth="base" borderRadius="base"><s-heading>{c.displayName}</s-heading><s-paragraph>{c.status} · {c._count.submissions} submissions · {c.handle}</s-paragraph><Form method="post"><input type="hidden" name="creatorId" value={c.id}/><input name="reason" aria-label="Reason" placeholder="Reason"/> <button name="status" value="APPROVED">Approve/Reinstate</button> <button name="status" value="SUSPENDED">Suspend</button> <button name="status" value="REJECTED">Reject</button></Form></s-box>)}</s-section></s-page>; }
