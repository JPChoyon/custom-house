import type { ActionFunctionArgs } from "react-router"; import { authenticate } from "../shopify.server"; import db from "../db.server";
export async function action({ request }: ActionFunctionArgs) { const { shop } = await authenticate.webhook(request); await db.$transaction([db.session.deleteMany({ where: { shop } }), db.creatorApplication.deleteMany({ where: { shop } }), db.designSubmission.deleteMany({ where: { shop } }), db.creator.deleteMany({ where: { shop } }), db.shopConfig.deleteMany({ where: { shop } }), db.auditLog.deleteMany({ where: { shop } })]); return new Response(); }
