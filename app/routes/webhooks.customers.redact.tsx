import type { ActionFunctionArgs } from "react-router"; import { authenticate } from "../shopify.server"; import db from "../db.server"; import { normalizeCustomerGid } from "../services/helium-sync.server";
export async function action({ request }: ActionFunctionArgs) { const { shop, payload } = await authenticate.webhook(request); const customerId = normalizeCustomerGid((payload as { customer?: { id?: number | string } }).customer?.id ?? ""); const creator = await db.creator.findUnique({ where: { shop_customerId: { shop, customerId } } }); if (creator) await db.$transaction([db.creatorApplication.deleteMany({ where: { creatorId: creator.id } }), db.designSubmission.deleteMany({ where: { creatorId: creator.id } }), db.creator.delete({ where: { id: creator.id } })]); return new Response(); }
