import type { ActionFunctionArgs, LoaderFunctionArgs } from "react-router"; import { Form, useLoaderData } from "react-router"; import db from "../db.server"; import { authenticate } from "../shopify.server"; import { changeCreatorStatus } from "../services/creator.server"; import { AdminGraphqlClient } from "../services/shopify-graphql.server";
export async function loader({ request }: LoaderFunctionArgs) { const { session } = await authenticate.admin(request); return db.creatorApplication.findMany({ where: { shop: session.shop }, include: { creator: true }, orderBy: { createdAt: "desc" }, take: 100 }); }
export async function action({ request }: ActionFunctionArgs) { const { session, admin } = await authenticate.admin(request); const form = await request.formData(); const creatorId = String(form.get("creatorId")); const intent = String(form.get("intent")); if (intent !== "APPROVED" && intent !== "REJECTED") throw new Response("Invalid action", { status: 400 }); await changeCreatorStatus(session.shop, creatorId, intent, new AdminGraphqlClient(admin), String(form.get("reason") || "") || undefined); return { ok: true }; }
export default function Applications() { const rows = useLoaderData<typeof loader>(); return <s-page heading="Applications"><s-section>{rows.length ? rows.map((item) => <s-box key={item.id} padding="base" borderWidth="base" borderRadius="base"><s-heading>{item.creator.displayName}</s-heading><s-paragraph>{item.status} · {new Date(item.createdAt).toLocaleDateString()}</s-paragraph><Form method="post"><input type="hidden" name="creatorId" value={item.creatorId}/><label>Decision note <input name="reason"/></label> <button name="intent" value="APPROVED">Approve</button> <button name="intent" value="REJECTED">Reject</button></Form></s-box>) : <s-paragraph>No applications.</s-paragraph>}</s-section></s-page>; }
