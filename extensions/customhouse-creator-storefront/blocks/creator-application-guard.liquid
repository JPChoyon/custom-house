{% assign protected_handle = block.settings.page_handle | default: 'become-a-creator' | strip %}
{% assign creator_dashboard_url = block.settings.dashboard_url | default: '/pages/creator-dashboard' %}
{% assign creator_dashboard_return_to = creator_dashboard_url | url_encode %}
{% assign creator_dashboard_login_url = '/customer_authentication/login?return_to=' | append: creator_dashboard_return_to %}

{% if request.page_type == 'page' and page.handle == protected_handle %}
  {{ 'creator-application.css' | asset_url | stylesheet_tag }}

  <style>
    main,
    #MainContent {
      visibility: hidden;
    }
  </style>

  {% if customer %}
    <span
      data-creator-application-guard
      data-status-endpoint="/apps/customhouse/api/creator-dashboard"
      data-dashboard-url="{{ creator_dashboard_url }}"
      hidden
    ></span>
    <script type="module" src="{{ 'creator-application-guard.js' | asset_url }}"></script>
    <noscript>
      <style>
        main,
        #MainContent {
          visibility: visible;
        }
      </style>
    </noscript>
  {% else %}
    <script>
      window.location.replace(
        {{ creator_dashboard_login_url | json }}
      );
    </script>
    <noscript>
      <meta
        http-equiv="refresh"
        content="0;url={{ creator_dashboard_login_url }}"
      >
    </noscript>
  {% endif %}
{% endif %}

{% schema %}
{
  "name": "Creator application guard",
  "target": "body",
  "settings": [
    {
      "type": "text",
      "id": "page_handle",
      "label": "Creator application page handle",
      "default": "become-a-creator"
    },
    {
      "type": "url",
      "id": "dashboard_url",
      "label": "Creator dashboard page"
    }
  ]
}
{% endschema %}
